let FastName = prompt("What is your name?");
let PartnerName = prompt("What is your partner name?");

// Calculation
let number = Math.random() * 100;
number = Math.floor(number) + 1;

// Showing Result
alert(
  FastName + " and " + PartnerName + "'s love percentage is " + number + "%"
);
